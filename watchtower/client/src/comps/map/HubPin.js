import { CloudUploadOutlined } from '@ant-design/icons';
const HubPin = () => (
    <div className="pin">
      <CloudUploadOutlined style={{ fontSize: '32px', color: 'red' }}/>
    </div>
  )

export default HubPin;